#include<stdio.h>
int main(){
	int i,n;
	scanf("%d",&n);
	int a[n],b[n];//size should be same 
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		b[i]=a[i]; //element by element 
	}
	printf("The copied Array is :\n");
	for(i=0;i<n;i++){
		printf("%d ",b[i]);
	}
	return 0;
}