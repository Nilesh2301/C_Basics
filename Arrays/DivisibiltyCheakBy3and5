#include <stdio.h>
int main(){
	int n,i,sum=0;
	printf("Enter the No. of Elements");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		if(a[i]%3==0 && a[i]%5==0){
			sum=sum+a[i];
		}
	}
	printf("\n sum of divi by 3 and 5 : %d\n",sum);
	
	
}