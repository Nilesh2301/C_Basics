#include<stdio.h>
int main()
{
    int a,b,c,d,e;
    double avg;
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    avg=(a+b+c+d+e)/5.0;// avg=(a+b+c+d+e)/(float)5;
    printf("Average=%.2lf",avg);
    return 0;
}