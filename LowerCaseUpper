// You are using GCC
#include<stdio.h>
int main()
{
    char ch;
    scanf("%c",&ch);
    //if(ch>='A' && ch<='Z')
    if(ch>=65 && ch<=90)
    {
     ch=ch+32;
    }
    printf("Character in lower case is %c",ch);
    return 0;
}
