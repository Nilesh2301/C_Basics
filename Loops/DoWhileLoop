

#include<stdio.h>
int main()
{
	char choice;
	int side;
    do
    {
	scanf("%d",&side);
	printf("Area of square is %d\n",side*side);
	printf("DO you want to continue:");
	scanf(" %c",&choice);
    }while(choice=='y'||choice=='Y');
	return 0;
}