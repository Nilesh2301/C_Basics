#include <stdio.h>
int main(){
	int n,i,sum=0;
	float ave;
	printf("Enter the No. of Elements");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		sum=sum+a[i];
	}
	ave=(float)sum/n;
	printf("\n ave : %.2f\n",ave);
	
	
}