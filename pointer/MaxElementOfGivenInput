#include <stdio.h>
#include <stdlib.h>
int main(){
	int *p,n,i,max;
	scanf("%d",&n);
	p=(int*)malloc(n*sizeof(int));
	for(i=0;i<n;i++){
		scanf("%d",p+i);
	
	}
	max=p[0];
	for(i=1;i<n;i++){
		if(max<p[i]){
			max=*(p+i);
		}
	}
	printf("Largest No. Is= %d",max);
}