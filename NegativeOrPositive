#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    //(n>=0)?printf("No. is +ve"):printf("No. is -ve");
    (n>=0)?(n>0)?printf("+ve"):printf("No. is zero"):printf("-ve");
    return 0;
}
